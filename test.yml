---
  - name: Check the remote host uptime 3
    hosts: all
    tasks:
      - name: Execute the Uptime command over Command module
        register: uptimeoutput
        command: "uptime"
      - name: check status
        yum:
         name: docker
         state: present
