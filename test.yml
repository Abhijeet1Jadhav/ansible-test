- name: httpd is1  there
    hosts: all
    tasks:
      - name: ensure httpd is installed
        yum: name=apache2 state=started
      - name : check if docker is installed
        yum:
         name: docker
         status: present
      - name: testing
        command: systemctl status firewalld
